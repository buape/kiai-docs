# Export Command

## Syntax

```
/export
```

## Permissions

This command is available to users with the Manage Server permission.

## Description

The `/export` command generates a JSON file with the XP and message count of each user in Kiai's database. This file can be used to import the data into another bot.
